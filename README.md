#include<iostream>
using namespace std;
int main()
{
  bool raining;
  cout<<"is it raining?/n";
  cout<<"enter 1 if yes\n enter 0 if no";
  cin>>raining;
  if(raining)
  {cout<<"rain detected/n";
    cout<<"opening rain shed";}
    else { cout<<"rain isn't detected/n";
      cout<<"closing shed";}
    }
  
